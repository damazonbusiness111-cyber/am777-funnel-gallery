# CRT Theme — Dev Environment

Next.js 16 / React 19 / Tailwind v4 — mirrors the CRT intro setup from `workspace env`.

```
npm install
npm run dev
```

Files:
- `src/components/CrtIntro.tsx` — standalone component, props with defaults, no external deps.
- `src/app/globals.css` — theme tokens + `.crt-*` styles.
- `crt-theme.html` — static single-file reference (no build step).
- `bukoshakepapaya-onboarding-context.md` — unrelated context saved earlier per request.

`CLAUDE.md` enables the token-optimizer skill for this project.
